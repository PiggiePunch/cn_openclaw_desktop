# 贡献指南

感谢你参与 OpenClaw CN Desktop。

## 开始前

1. 先阅读 [README.md](./README.md) 了解项目结构和开发命令。
2. 新功能建议先提 Issue，避免重复开发。
3. Bug 修复请尽量附复现步骤和日志。

## 开发流程

1. Fork 仓库并创建分支（建议：`feat/...`、`fix/...`）。
2. 完成代码修改并本地自测。
3. 提交 Pull Request，说明变更动机、实现方案、测试结果。

## 建议的本地检查

```bash
pnpm build
pnpm tauri:build:debug
```

如改动涉及运行时逻辑，建议补充：

- 关键页面手动回归（聊天、智能体、定时任务、通道）
- 关键系统场景（Gateway 重启、配置保存、会话切换）

## 提交信息建议

推荐中文动词开头，清晰描述范围，例如：

- `修复定时任务绑定已删除智能体导致失败`
- `优化会话列表长标题显示与删除按钮可见性`
- `新增通道配置自动清理无效智能体绑定`

## Pull Request 需要包含

- 变更摘要（做了什么）
- 背景与原因（为什么）
- 影响范围（哪些模块/页面）
- 测试说明（如何验证）
- 截图或录屏（如涉及 UI）

## 代码风格

- 前端：保持现有 React + Tailwind 风格
- Rust：保持模块边界清晰，尽量小函数
- 不要混入与本次任务无关的大范围重构

## 文档变更

当你修改以下内容时，请同步更新文档：

- 命令、脚本、构建流程
- 配置结构与默认行为
- 用户可见功能（UI 或交互）

